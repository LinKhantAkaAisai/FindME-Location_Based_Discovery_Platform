{
  "name": "findme-monorepo",
  "version": "1.0.0",
  "description": "Location-based discovery platform Monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "server": "npm run dev --workspace=server",
    "mobile": "npm start --workspace=findme-mobile"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2"
  }
}